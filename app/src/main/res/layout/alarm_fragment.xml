<?xml version="1.0" encoding="utf-8"?>

<!-- This fragment is displayed when the tab selected by the user is set to Alarms
TODO: Establish connection to a dataset to enable empty state that shows text and image when no cards/alarm objects exist
-->
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/coordinatorLayoutFile"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.kelvinharron.qralarm.AlarmsFragment">


    <android.support.v7.widget.RecyclerView
        android:id="@+id/alarmListRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="100dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        app:layout_scrollFlags="scroll|enterAlways" />

</android.support.design.widget.CoordinatorLayout>

    <!--
    TODO: check if recycler view data is empty and if so, display the below layout instead of a blank screen
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_horizontal"
                android:paddingTop="140dp"
                android:text="@string/empty_state"
                android:textColor="@color/textColorEmptyState"
                android:textSize="25dp"
                android:textStyle="normal" />

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:src="@mipmap/ic_launcher" />

        </LinearLayout>

    -->